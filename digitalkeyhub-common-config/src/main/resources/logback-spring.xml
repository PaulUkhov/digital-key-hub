<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Конфигурация для разработки (без Docker) -->
    <springProfile name="!docker">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- Конфигурация для Docker окружения -->
    <springProfile name="docker">
        <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
            <destination>logstash:5000</destination>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <customFields>{"service":"digitalkeyhub","environment":"${SPRING_PROFILES_ACTIVE:-default}"}</customFields>
                <includeContext>false</includeContext>
                <timeZone>UTC</timeZone>
            </encoder>
            <keepAliveDuration>5 minutes</keepAliveDuration>
            <connectionTimeout>5000</connectionTimeout>
            <reconnectionDelay>3000</reconnectionDelay>
        </appender>

        <!-- Асинхронная отправка логов в Logstash -->
        <appender name="ASYNC_LOGSTASH" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="LOGSTASH" />
            <queueSize>1024</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <includeCallerData>false</includeCallerData>
        </appender>
    </springProfile>

    <root level="INFO">
        <springProfile name="!docker">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <springProfile name="docker">
            <appender-ref ref="ASYNC_LOGSTASH"/>
        </springProfile>
    </root>

    <!-- Логирование SQL запросов -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Логирование параметров SQL -->
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Уменьшение логов от Spring -->
    <logger name="org.springframework" level="WARN"/>
</configuration>